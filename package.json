{
  "name": "no-ide",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "No-ide — 面向操作和结果的轻量代码运行工作台",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node --check src/app.js && node --check src/catalog.js && node --check src/project-model.js && node --check src/source-control.js && node --check src/main.js",
    "test": "node --test tests/models.test.js"
  },
  "engines": { "node": "^20.19.0 || >=22.12.0" },
  "dependencies": { "quasar": "2.33.0", "vue": "3.5.43" },
  "devDependencies": { "vite": "8.3.0" }
}
