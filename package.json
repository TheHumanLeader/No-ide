{
  "name": "no-ide",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "No-ide — 面向操作和结果的轻量代码运行工作台",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node --check src/app.js && node --check src/main.js"
  },
  "engines": { "node": "^20.19.0 || >=22.12.0" },
  "dependencies": { "quasar": "^2.32.3", "vue": "^3.5.13" },
  "devDependencies": { "vite": "^8.0.0" }
}
