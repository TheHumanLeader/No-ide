# No-ide 协作规则

1. 先读 `prd/README.md`、`prd/design/运行台.md` 和 `prd/verification.md`。
2. 用户优先关心操作和结果，不把实现复杂度转嫁给用户；默认中文、明亮粉色。
3. 前端使用 Vue 3 / Quasar；正式入口在 `src/main.js`。
4. `src/app.js` 目前只包含预览状态适配器。模拟结果必须持续标注，不冒充真实工具检测、构建、设备连接或性能数据。
5. 构建、更新、就绪必须分阶段；更新失败保留旧版本；取消操作必须使过期回调失效。
6. 任务、日志、消息队列必须有界。不添加空转定时器、全工程扫描、默认所有语言服务或无限日志 DOM。
7. 真实执行器只管理自身创建的受控进程；执行项目信任确认，保护环境变量和令牌。
8. 修改后做语法与浏览器交互检查。离线兼容控件测试不能替代正式 Quasar 构建验证；失败和未验证项必须记录。
9. 不把实现目标改写为已完成能力。后端未接入前，不添加虚构 CPU、内存、端口检测和设备信息。
10. 代码、设计说明和验证记录同步维护。

## v0.2 补充

- 继续修改前先读 `prd/project-instance-vcs.md` 与 `prd/verification-v0.2.md`。
- 项目持有配置与仓库；实例引用共享配置，仅覆盖端口、参数、环境与状态。项目级操作不得影响其他项目。
- 目录以选择器为主，不把浏览器目录名伪造成绝对路径，不遍历 / 上传目录内容。
- Git 本地提交与推送分开；SVN 勾选提交直接远端，不伪造 Git 暂存区。未选改动保留。
- UI 中全部运行和 SCM 操作为演示；用户创建的项目必须显示待执行器，不能附赠假的仓库检测或 Diff。
- 检查 `npm run check`、`npm test` 和 `tests/interaction-v2.py`；原生目录选择器测试替身不等于本机 OS 测试。
