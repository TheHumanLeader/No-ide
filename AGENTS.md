# No-ide 协作规则

1. 先读 `prd/README.md`、`prd/project-instance-vcs.md`、`prd/native-v0.3.md` 与对应版本验证记录。
2. 用户优先关心操作和结果，默认中文、明亮粉色，不把内部实现复杂度转嫁给用户。
3. 前端使用 Vue 3 / Quasar。`src/main.js` 区分本机真实执行器与静态演示。
4. `src/app.js` 等旧页面是 v0.2 预览适配器，模拟结果必须标注。`src/live-*` 连接真实 Rust API，不得用模拟数据兜底真实操作失败。
5. `backend/` 为 Rust 本地服务，必须保持 Linux / Windows / macOS 的平台边界，不把一个平台编译通过说成三平台验证通过。
6. 构建、更新、启动与就绪分阶段。构建失败不主动停止旧进程；未做原子产物发布时，不承诺旧应用依赖的全部磁盘文件保持不变。
7. 命令、日志和推送队列必须有界；只监听运行中的配置和明确源路径，不全盘扫描、不默认启动全语言服务。
8. Windows 受控进程使用 Job Object，Unix 使用进程组。停止任务应清理普通子进程，不误杀其他应用。
9. Git/SVN 优先级：项目指定 > 全局指定 > 自动发现。手动路径失效不静默切换。检测是检查真实命令行客户端，不是伪造后台服务。
10. VCS 写操作先准备确认，执行前复验。不得悄悄强制推送、重置、丢弃修改；Git 本地提交和远端推送分离。
11. 安全边界包括 loopback、Host / Origin 检查、会话认证、项目信任、根目录约束和敏感字段保护，不默认开放远程执行。
12. 真实 API 验证使用 `tests/backend-smoke.py`；真实前端验证使用 `tests/native-browser.py`。全部测试写入临时本地仓库，不触碰用户业务数据。
13. 代码、设计说明、限制与验证结果同步维护。以实际 CI / 报告为准，不提前宣布成功。系统图形选择器、外部认证服务、长期资源占用等未测项需明确。
14. 保留实际构建包版本与提交信息；不把 GitHub Pages 的静态演示描述为能独立运行本机项目。
